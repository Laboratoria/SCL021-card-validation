const userInput = document.getElementById("cardNum");


const validator = {

  isValid:(userInput) => {

    // convertir data en array tipo number e invertir
    let numData = userInput.split("");
    numData.map(Number);
    numData.reverse();
    
  }
 
};

export default validator;

// function validarformulario(){

  var tarjeta valida, tarjeta invalida;
  tarjeta valida =document.getElementById('tarjeta valida').value;
  tarjeta invalida =document.getElementById('tarjeta invalida').value;

  if 16-19 13-16
  
//
def validarFuction( num);
  if pos.isdigit()==True:
      dgt+=1
    cn+=1
 if dgt !=16:
 raise digitosError("Error en numero de digitos")
 else: 
 print ("Numero de tarjeta valido")
with open ('index.js')
